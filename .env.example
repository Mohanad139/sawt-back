# Sawt Voice Agent — Environment Variables
# Copy to .env and fill in your keys:  cp .env.example .env

# ── Required ─────────────────────────────────────────────
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
OPENAI_API_KEY=

# ── Required per provider ────────────────────────────────
ELEVEN_API_KEY=           # required if TTS_PROVIDER=elevenlabs (default)
# DEEPGRAM_API_KEY=       # required if STT_PROVIDER=deepgram

# ── Optional (defaults shown) ────────────────────────────
# STT_PROVIDER=openai              # openai or deepgram
# STT_MODEL=whisper-1              # OpenAI Whisper model
# STT_LANGUAGE=                    # empty = auto-detect, or "ar" for Arabic-only
# LLM_MODEL=gpt-4o
# LLM_TEMPERATURE=0.3
# TTS_PROVIDER=elevenlabs          # elevenlabs or openai
# TTS_MODEL=eleven_flash_v2_5
# TTS_VOICE_ID=EXAVITQu4vr4xnSDxMaL
# TTS_LANGUAGE=ar
# LOG_LEVEL=INFO
